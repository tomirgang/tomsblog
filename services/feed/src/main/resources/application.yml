spring:
  application:
    name: feed

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration/feed

  kafka:
    consumer:
      properties:
        spring.json.trusted.packages: "de.tomsblog.events"
