apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-management
  namespace: tomsblog
  labels:
    app: user-management
spec:
  selector:
    matchLabels:
      app: user-management
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
