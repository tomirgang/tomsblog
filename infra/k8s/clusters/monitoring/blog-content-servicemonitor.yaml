apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: blog-content
  namespace: tomsblog
  labels:
    app: blog-content
spec:
  selector:
    matchLabels:
      app: blog-content
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
