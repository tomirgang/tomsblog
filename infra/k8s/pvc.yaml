apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blog-storage
  namespace: tomsblog
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: hcloud-volumes
