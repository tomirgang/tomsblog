apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluster-redirect-tls
  namespace: tomsblog
spec:
  secretName: cluster-redirect-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - cluster.tomirgang.de
